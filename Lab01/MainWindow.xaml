<Window x:Class="Lab01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab01"
        mc:Ignorable="d"
        Title="Laboratorio 1" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" 
               x:Name="lblMessages"/>
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button x:Name="btnStartTask" 
                    Content="Iniciar tarea" 
                    Margin="5" 
                    Padding="5" 
                    Click="BtnStartTask_OnClick"/>
            <Button x:Name="CancelTask" 
                    Content="Cancelar tarea"
                    Margin="5" 
                    Padding="5"
                    Click="CancelTask_OnClick"/>
            <Button x:Name="btnShowStatus"
                    Content="Mostrar estatus"
                    Margin="5"
                    Padding="5"
                    Click="BtnShowStatus_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
